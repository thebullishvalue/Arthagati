# ──────────────────────────────────────────────────────────────────────────────
# ARTHAGATI — Python Dependencies
# @thebullishvalue · v2.5.0
# ──────────────────────────────────────────────────────────────────────────────
#
# Install:  pip install -r requirements.txt
# Python:   >= 3.10  (required for built-in type hints: dict[...], int | None)
#

# Web application framework
streamlit       >= 1.32.0

# Data manipulation
pandas          >= 2.1.0
numpy           >= 1.26.0
scipy           >= 1.11.0

# Charting
plotly          >= 5.20.0

# HTTP requests (Google Sheets via direct URL)
requests        >= 2.31.0

# Timezone utilities
pytz            >= 2024.1

# Environment variable management (optional, for .env files)
python-dotenv   >= 1.0.0
