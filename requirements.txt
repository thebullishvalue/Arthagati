# ──────────────────────────────────────────────────────────────────────────────
# ARTHAGATI — Python Dependencies
# @thebullishvalue — see config.VERSION for the authoritative version
# ──────────────────────────────────────────────────────────────────────────────
#
# Install:  pip install -r requirements.txt
# Python:   >= 3.10  (required for built-in type hints: dict[...], int | None)
#

# Web application framework.
#
# Lower bound is 1.50, set by what the UI actually calls:
#   st.navigation / st.Page      (1.36) — the nav rail
#   st.container(key=...)        (1.40) — the panel primitive theme.css hooks
#                                          via `.st-key-panel-*`
#   st.segmented_control         (1.40) — the window and appearance controls
#   width="stretch"              (1.50) — replaces use_container_width, which
#                                          is removed after 2025-12-31
# DEVELOP ON THIS EXACT VERSION TOO. `pip install -r requirements.txt` gives it
# to you. A local interpreter on a different Streamlit is precisely how this
# reached a deploy.
streamlit==1.52.2

# Data manipulation
pandas          >= 2.1.0
numpy           >= 1.26.0
scipy           >= 1.11.0

# Charting
plotly          >= 5.20.0

# HTTP requests (Google Sheets via gviz API)
requests        >= 2.31.0

# Timezone utilities
pytz            >= 2024.1

# Cross-platform ANSI colour support for the structured terminal logger
colorama        >= 0.4.6

